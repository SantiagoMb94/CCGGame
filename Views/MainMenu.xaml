<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CCGGame.Views.MainMenu">

    <StackLayout>
        <Label Text="CCG Game" FontSize="Large" HorizontalOptions="Center" />
        <Button Text="Start New Game" Command="{Binding StartNewGameCommand}" />
        <Button Text="Load Game" Command="{Binding LoadGameCommand}" />
    </StackLayout>

</ContentPage>
